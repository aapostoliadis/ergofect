interface:
  display_name: "Ergofect"
  short_description: "Repo-specific patterns and workflows for ergofect"
  default_prompt: "Use the ergofect repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true